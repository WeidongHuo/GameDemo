# GameDemo

Static demo site for the StillWater game showcase.

The StillWater build zip is distributed through the `v0.1.0` GitHub Release because GitHub repositories block regular files larger than 100 MiB.
